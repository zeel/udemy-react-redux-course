import React, { useContext } from "react";
import Field from "./Field";
import Button from "./Button";

export default () => {
  return (
    <div>
      <Field />
      <Button />
    </div>
  );
};
